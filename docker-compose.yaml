include:
  - path: deps.docker-compose.yaml

services:
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - 3001:3001
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    ports:
      - 4000:4000
